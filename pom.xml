<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>br.com.reqsys</groupId>
    <artifactId>reqsys-java-platform</artifactId>
    <version>1.0.0-java-padrao-ouro</version>
    <packaging>pom</packaging>
    <name>ReqSys Java Enterprise Platform</name>

    <properties>
        <java.version>21</java.version>
        <spring-boot.version>3.3.6</spring-boot.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <modules>
        <module>libs/reqsys-common</module>
        <module>libs/reqsys-observability</module>
        <module>libs/reqsys-security</module>
        <module>libs/reqsys-microsoft-graph</module>
        <module>apps/reqsys-enterprise-api</module>
        <module>apps/forms-planner-orchestrator</module>
        <module>apps/planner-teams-status-worker</module>
        <module>apps/cadastra-pf-siopi-api</module>
        <module>apps/report-services-api</module>
        <module>apps/redmine-copilot-sync</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
